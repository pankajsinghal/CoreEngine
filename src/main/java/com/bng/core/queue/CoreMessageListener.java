/*package com.bng.core.queue;

import javax.jms.Message;
import javax.jms.MessageListener;

public class CoreMessageListener implements MessageListener {
	
	MessageSender sender;

	/**
	 * Implementation of <code>MessageListener</code>.
	 */
	/*public void onMessage(Message message) {
		try {
			//TODO to be handled by richa
			Logger.sysLog(LogValues.info, this.getClass().getName(),"[end point]Message received - " + message);

		} catch (Exception e) {
			Logger.sysLog(LogValues.error, this.getClass().getName(),e.getMessage());
		}
	}

	public MessageSender getSender() {
		return sender;
	}

	public void setSender(MessageSender sender) {
		this.sender = sender;
	}

}*/
